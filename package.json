{
  "name": "whatsappbot",
  "version": "1.0.0",
  "description": "make bot whatsapp",
  "main": "index.js",
  "scripts": {
    "start": "npm install && node index.js", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ricky",
  "license": "ISC",
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.23.0"
  }
}
